language: python
python:
  - 3.6
  - 3.7
cache: pip
before_install:
  - pip install pytest==4.5.0
  - pip install codecov==2.0.15
  - pip install pytest-cov==2.7.1
install: pip install -e .
script: pytest --cov-report=xml --cov=ukat
after_success: 
  - cd ukat
  - codecov --token="79d35a7a-cca0-4770-b1f4-3ff622604c64"