language: python
python:
  - 3.6
  - 3.7
cache: pip
before_install:
  - pip install pytest
  - pip install codecov
  - pip install pytest-cov
install: pip install -e .
script: pytest --cov-report=xml --cov=ukat
after_success: 
  - cd ukat/
  - codecov -t 79d35a7a-cca0-4770-b1f4-3ff622604c64